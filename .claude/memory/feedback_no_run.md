---
name: feedback_no_run
description: User runs the application manually — do not start the dev server or run the app in the session
metadata:
  type: feedback
---

Do not run the application (e.g. `npm run dev`, `next dev`) during the session.

**Why:** User prefers to run the app manually themselves.

**How to apply:** Skip any "start dev server" or "verify in browser" steps. Make code changes only, and let the user test by running the app themselves.
